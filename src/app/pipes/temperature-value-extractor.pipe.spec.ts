import { TemperatureValueExtractorPipe } from './temperature-value-extractor.pipe';

describe('TemperatureValueExtractorPipe', () => {
  it('create an instance', () => {
    const pipe = new TemperatureValueExtractorPipe();
    expect(pipe).toBeTruthy();
  });
});
